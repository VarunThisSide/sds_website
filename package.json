{
  "name": "sds_blog",
  "version": "1.0.0",
  "description": "blog page for sds website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "kill-port 8000 && nodemon -r --experimental-json-modules index.js"
  },
  "author": "Anant",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.0",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "nodemon": "^3.1.7",
    "prettier": "^3.4.1"
  }
}
